// import 'package:conversor_de_moeda/models/conversion.dart';
// import 'package:flutter_test/flutter_test.dart';

// void main() {
//   group('Conversion', () {
//     test('should create a Conversion instance', () {
//       final conversion = Conversion(rate: 0.85);

//       expect(conversion, isA<Conversion>());
//       expect(conversion.rate, 0.85);
//     });

//     test('should throw an error if rate is negative', () {
//       expect(() => Conversion(rate: -1.0), throwsA(isA<AssertionError>()));
//     });

//     test('should convert rate correctly', () {
//       final conversion = Conversion(rate: 0.85);

//       expect(conversion.rate, equals(0.85));
//     });
//   });
// }
